## What

<!-- One or two sentences. Link the issue this closes, if there is one. -->

## How it was tested

<!-- `npm run typecheck && npm run build`, then what you did in Chrome. `yurei call <tool> '<json>'` runs one tool by hand. -->

## Checklist

- [ ] The title is a [Conventional Commit](https://www.conventionalcommits.org) (`feat:`, `fix:`, `docs:`, ...)
- [ ] `npm run typecheck` and `npm run build` pass
- [ ] Tested in Chrome with the rebuilt extension (`yurei reload-extension`)
- [ ] No screenshots or logs with personal data
